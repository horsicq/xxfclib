/* Copyright (c) 2026 hors<horsicq@gmail.com>
 * SPDX-License-Identifier: MIT
 */

/* xx_starkit_extractor.c - search raw data for Starkit.
 *
 * Scans for:
 *   4A 4C 1A 00 at +0  ("JL..")
 * Offset 0 is always tried as well.
 * Each candidate must be accepted by the starkit reader, which also measures it,
 * and named by the detector, both on a view that starts at the candidate.
 * See xx_format_extractor_engine.h.
 *
 * GENERATED by gen_extractors.py from the detector's prefilters.
 */

#include "../xx_format_extractor_engine.h"
#include "xxfclib/formats/starkit/xx_starkit.h"

static const uint8_t k_anchor0[] = { 0x4A, 0x4C, 0x1A, 0x00 };

static const xx_format_search_anchor k_anchors[] = {
    { k_anchor0, sizeof(k_anchor0), 0U },
};

static const xx_file_type_t k_types[] = { XX_FILE_TYPE_STARKIT };

static Abstractformat *xx_starkit_search_open(xx_io_device *window) {
    xx_starkit *reader = xx_starkit_create(window, 0);
    return reader ? &reader->format : NULL;
}

static void xx_starkit_search_close(Abstractformat *format) {
    /* The format is the first member, so this is the reader itself. */
    xx_starkit_free((xx_starkit *)format);
}

static const xx_format_search_desc k_desc = {
    k_types, sizeof(k_types) / sizeof(k_types[0]),
    k_anchors, sizeof(k_anchors) / sizeof(k_anchors[0]),
    xx_starkit_search_open, xx_starkit_search_close
};

static xx_format_search_state *xx_starkit_create_format_search(
    xx_format_extractor *self, xx_io_device *device, const xx_list_s *options,
    xx_pd_struct *pd) {
    (void)self;
    return xx_format_search_create(&k_desc, device, options, pd);
}

static const xx_format_search_info *xx_starkit_get_current_format_info(
    xx_format_extractor *self, xx_format_search_state *state) {
    (void)self;
    return xx_format_search_current(state);
}

static bool xx_starkit_format_search_find_next(xx_format_extractor *self,
                                          xx_format_search_state *state,
                                          xx_pd_struct *pd) {
    (void)self;
    return xx_format_search_find_next(state, pd);
}

static void xx_starkit_free_format_search(xx_format_extractor *self,
                                     xx_format_search_state *state) {
    (void)self;
    xx_format_search_free(state);
}

xx_format_extractor xx_starkit_extractor = {
    xx_starkit_create_format_search,
    xx_starkit_get_current_format_info,
    xx_starkit_format_search_find_next,
    xx_starkit_free_format_search
};
